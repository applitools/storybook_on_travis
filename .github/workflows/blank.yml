# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the action will run. 
on:
  # Triggers the workflow on push or pull request events but only for the master branch
#   push:
#     branches: [ master ]
#   pull_request:
#     branches: [ master ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build1:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    env:
      APPLITOOLS_API_KEY: ${{ secrets.APPLITOOLS_API_KEY }}
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2
      - name: Use Node.js
        uses: actions/setup-node@v1
        with:
          node-version: '15.x'
      - run: echo ${{ secrets.APPLITOOLS_API_KEY }}
      - run: cd $GITHUB_WORKSPACE
      - run: npm install --save-dev @applitools/eyes-storybook
      - run: touch applitools.config.1.js
      - run: sed 's+{concurrency}+50+g' config.file.template | sed 's+{buildName}+LoadTest1+g' | sed 's+{testServer}+https://testeyes.applitools.com+g' > applitools.config.1.js
      - run: npx eyes-storybook --conf applitools.config.1.js
  # build2:
  #   # The type of runner that the job will run on
  #   runs-on: ubuntu-latest
  #   env:
  #     APPLITOOLS_API_KEY: ${{ secrets.APPLITOOLS_API_KEY }}
  #   # Steps represent a sequence of tasks that will be executed as part of the job
  #   steps:
  #     # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
  #     - uses: actions/checkout@v2
  #     - name: Use Node.js
  #       uses: actions/setup-node@v1
  #       with:
  #         node-version: '15.x'
  #     - run: echo ${{ secrets.APPLITOOLS_API_KEY }}
  #     - run: cd $GITHUB_WORKSPACE
  #     - run: npm install --save-dev @applitools/eyes-storybook
  #     - run: touch applitools.config.2.js
  #     - run: sed 's+{concurrency}+50+g' config.file.template | sed 's+{buildName}+LoadTest2+g' | sed 's+{testServer}+https://testeyes.applitools.com+g' > applitools.config.2.js
  #     - run: npx eyes-storybook --conf applitools.config.2.js
  # build3:
  #   # The type of runner that the job will run on
  #   runs-on: ubuntu-latest
  #   env:
  #     APPLITOOLS_API_KEY: ${{ secrets.APPLITOOLS_API_KEY }}
  #   # Steps represent a sequence of tasks that will be executed as part of the job
  #   steps:
  #     # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
  #     - uses: actions/checkout@v2
  #     - name: Use Node.js
  #       uses: actions/setup-node@v1
  #       with:
  #         node-version: '15.x'
  #     - run: echo ${{ secrets.APPLITOOLS_API_KEY }}
  #     - run: cd $GITHUB_WORKSPACE
  #     - run: npm install --save-dev @applitools/eyes-storybook
  #     - run: touch applitools.config.3.js
  #     - run: sed 's+{concurrency}+50+g' config.file.template | sed 's+{buildName}+LoadTest3+g' | sed 's+{testServer}+https://testeyes.applitools.com+g' > applitools.config.3.js
  #     - run: npx eyes-storybook --conf applitools.config.3.js
  # build4:
  #   # The type of runner that the job will run on
  #   runs-on: ubuntu-latest
  #   env:
  #     APPLITOOLS_API_KEY: ${{ secrets.APPLITOOLS_API_KEY }}
  #   # Steps represent a sequence of tasks that will be executed as part of the job
  #   steps:
  #     # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
  #     - uses: actions/checkout@v2
  #     - name: Use Node.js
  #       uses: actions/setup-node@v1
  #       with:
  #         node-version: '15.x'
  #     - run: echo ${{ secrets.APPLITOOLS_API_KEY }}
  #     - run: cd $GITHUB_WORKSPACE
  #     - run: npm install --save-dev @applitools/eyes-storybook
  #     - run: touch applitools.config.4.js
  #     - run: sed 's+{concurrency}+50+g' config.file.template | sed 's+{buildName}+LoadTest4+g' | sed 's+{testServer}+https://testeyes.applitools.com+g' > applitools.config.4.js
  #     - run: npx eyes-storybook --conf applitools.config.4.js
  # build5:
  #   # The type of runner that the job will run on
  #   runs-on: ubuntu-latest
  #   env:
  #     APPLITOOLS_API_KEY: ${{ secrets.APPLITOOLS_API_KEY }}
  #   # Steps represent a sequence of tasks that will be executed as part of the job
  #   steps:
  #     # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
  #     - uses: actions/checkout@v2
  #     - name: Use Node.js
  #       uses: actions/setup-node@v1
  #       with:
  #         node-version: '15.x'
  #     - run: echo ${{ secrets.APPLITOOLS_API_KEY }}
  #     - run: cd $GITHUB_WORKSPACE
  #     - run: npm install --save-dev @applitools/eyes-storybook
  #     - run: touch applitools.config.5.js
  #     - run: sed 's+{concurrency}+50+g' config.file.template | sed 's+{buildName}+LoadTest5+g' | sed 's+{testServer}+https://testeyes.applitools.com+g' > applitools.config.5.js
  #     - run: npx eyes-storybook --conf applitools.config.5.js
  # build6:
  #   # The type of runner that the job will run on
  #   runs-on: ubuntu-latest
  #   env:
  #     APPLITOOLS_API_KEY: ${{ secrets.APPLITOOLS_API_KEY }}
  #   # Steps represent a sequence of tasks that will be executed as part of the job
  #   steps:
  #     # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
  #     - uses: actions/checkout@v2
  #     - name: Use Node.js
  #       uses: actions/setup-node@v1
  #       with:
  #         node-version: '15.x'
  #     - run: echo ${{ secrets.APPLITOOLS_API_KEY }}
  #     - run: cd $GITHUB_WORKSPACE
  #     - run: npm install --save-dev @applitools/eyes-storybook
  #     - run: touch applitools.config.6.js
  #     - run: sed 's+{concurrency}+50+g' config.file.template | sed 's+{buildName}+LoadTest6+g' | sed 's+{testServer}+https://testeyes.applitools.com+g' > applitools.config.6.js
  #     - run: npx eyes-storybook --conf applitools.config.6.js
  # build7:
  #   # The type of runner that the job will run on
  #   runs-on: ubuntu-latest
  #   env:
  #     APPLITOOLS_API_KEY: ${{ secrets.APPLITOOLS_API_KEY }}
  #   # Steps represent a sequence of tasks that will be executed as part of the job
  #   steps:
  #     # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
  #     - uses: actions/checkout@v2
  #     - name: Use Node.js
  #       uses: actions/setup-node@v1
  #       with:
  #         node-version: '15.x'
  #     - run: echo ${{ secrets.APPLITOOLS_API_KEY }}
  #     - run: cd $GITHUB_WORKSPACE
  #     - run: npm install --save-dev @applitools/eyes-storybook
  #     - run: touch applitools.config.7.js
  #     - run: sed 's+{concurrency}+50+g' config.file.template | sed 's+{buildName}+LoadTest7+g' | sed 's+{testServer}+https://testeyes.applitools.com+g' > applitools.config.7.js
  #     - run: npx eyes-storybook --conf applitools.config.7.js
  # build8:
  #   # The type of runner that the job will run on
  #   runs-on: ubuntu-latest
  #   env:
  #     APPLITOOLS_API_KEY: ${{ secrets.APPLITOOLS_API_KEY }}
  #   # Steps represent a sequence of tasks that will be executed as part of the job
  #   steps:
  #     # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
  #     - uses: actions/checkout@v2
  #     - name: Use Node.js
  #       uses: actions/setup-node@v1
  #       with:
  #         node-version: '15.x'
  #     - run: echo ${{ secrets.APPLITOOLS_API_KEY }}
  #     - run: cd $GITHUB_WORKSPACE
  #     - run: npm install --save-dev @applitools/eyes-storybook
  #     - run: touch applitools.config.8.js
  #     - run: sed 's+{concurrency}+50+g' config.file.template | sed 's+{buildName}+LoadTest8+g' | sed 's+{testServer}+https://testeyes.applitools.com+g' > applitools.config.8.js
  #     - run: npx eyes-storybook --conf applitools.config.8.js
  # build9:
  #   # The type of runner that the job will run on
  #   runs-on: ubuntu-latest
  #   env:
  #     APPLITOOLS_API_KEY: ${{ secrets.APPLITOOLS_API_KEY }}
  #   # Steps represent a sequence of tasks that will be executed as part of the job
  #   steps:
  #     # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
  #     - uses: actions/checkout@v2
  #     - name: Use Node.js
  #       uses: actions/setup-node@v1
  #       with:
  #         node-version: '15.x'
  #     - run: echo ${{ secrets.APPLITOOLS_API_KEY }}
  #     - run: cd $GITHUB_WORKSPACE
  #     - run: npm install --save-dev @applitools/eyes-storybook
  #     - run: touch applitools.config.9.js
  #     - run: sed 's+{concurrency}+50+g' config.file.template | sed 's+{buildName}+LoadTest9+g' | sed 's+{testServer}+https://testeyes.applitools.com+g' > applitools.config.9.js
  #     - run: npx eyes-storybook --conf applitools.config.9.js
  # build10:
  #   # The type of runner that the job will run on
  #   runs-on: ubuntu-latest
  #   env:
  #     APPLITOOLS_API_KEY: ${{ secrets.APPLITOOLS_API_KEY }}
  #   # Steps represent a sequence of tasks that will be executed as part of the job
  #   steps:
  #     # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
  #     - uses: actions/checkout@v2
  #     - name: Use Node.js
  #       uses: actions/setup-node@v1
  #       with:
  #         node-version: '15.x'
  #     - run: echo ${{ secrets.APPLITOOLS_API_KEY }}
  #     - run: cd $GITHUB_WORKSPACE
  #     - run: npm install --save-dev @applitools/eyes-storybook
  #     - run: touch applitools.config.10.js
  #     - run: sed 's+{concurrency}+50+g' config.file.template | sed 's+{buildName}+LoadTest10+g' | sed 's+{testServer}+https://testeyes.applitools.com+g' > applitools.config.10.js
  #     - run: npx eyes-storybook --conf applitools.config.10.js
  # build11:
  #   # The type of runner that the job will run on
  #   runs-on: ubuntu-latest
  #   env:
  #     APPLITOOLS_API_KEY: ${{ secrets.APPLITOOLS_API_KEY }}
  #   # Steps represent a sequence of tasks that will be executed as part of the job
  #   steps:
  #     # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
  #     - uses: actions/checkout@v2
  #     - name: Use Node.js
  #       uses: actions/setup-node@v1
  #       with:
  #         node-version: '15.x'
  #     - run: echo ${{ secrets.APPLITOOLS_API_KEY }}
  #     - run: cd $GITHUB_WORKSPACE
  #     - run: npm install --save-dev @applitools/eyes-storybook
  #     - run: touch applitools.config.11.js
  #     - run: sed 's+{concurrency}+50+g' config.file.template | sed 's+{buildName}+LoadTest11+g' | sed 's+{testServer}+https://testeyes.applitools.com+g' > applitools.config.11.js
  #     - run: npx eyes-storybook --conf applitools.config.11.js
  # build12:
  #   # The type of runner that the job will run on
  #   runs-on: ubuntu-latest
  #   env:
  #     APPLITOOLS_API_KEY: ${{ secrets.APPLITOOLS_API_KEY }}
  #   # Steps represent a sequence of tasks that will be executed as part of the job
  #   steps:
  #     # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
  #     - uses: actions/checkout@v2
  #     - name: Use Node.js
  #       uses: actions/setup-node@v1
  #       with:
  #         node-version: '15.x'
  #     - run: echo ${{ secrets.APPLITOOLS_API_KEY }}
  #     - run: cd $GITHUB_WORKSPACE
  #     - run: npm install --save-dev @applitools/eyes-storybook
  #     - run: touch applitools.config.12.js
  #     - run: sed 's+{concurrency}+50+g' config.file.template | sed 's+{buildName}+LoadTest12+g' | sed 's+{testServer}+https://testeyes.applitools.com+g' > applitools.config.12.js
  #     - run: npx eyes-storybook --conf applitools.config.12.js
  # build13:
  #   # The type of runner that the job will run on
  #   runs-on: ubuntu-latest
  #   env:
  #     APPLITOOLS_API_KEY: ${{ secrets.APPLITOOLS_API_KEY }}
  #   # Steps represent a sequence of tasks that will be executed as part of the job
  #   steps:
  #     # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
  #     - uses: actions/checkout@v2
  #     - name: Use Node.js
  #       uses: actions/setup-node@v1
  #       with:
  #         node-version: '15.x'
  #     - run: echo ${{ secrets.APPLITOOLS_API_KEY }}
  #     - run: cd $GITHUB_WORKSPACE
  #     - run: npm install --save-dev @applitools/eyes-storybook
  #     - run: touch applitools.config.13.js
  #     - run: sed 's+{concurrency}+50+g' config.file.template | sed 's+{buildName}+LoadTest13+g' | sed 's+{testServer}+https://testeyes.applitools.com+g' > applitools.config.13.js
  #     - run: npx eyes-storybook --conf applitools.config.13.js
  # build14:
  #   # The type of runner that the job will run on
  #   runs-on: ubuntu-latest
  #   env:
  #     APPLITOOLS_API_KEY: ${{ secrets.APPLITOOLS_API_KEY }}
  #   # Steps represent a sequence of tasks that will be executed as part of the job
  #   steps:
  #     # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
  #     - uses: actions/checkout@v2
  #     - name: Use Node.js
  #       uses: actions/setup-node@v1
  #       with:
  #         node-version: '15.x'
  #     - run: echo ${{ secrets.APPLITOOLS_API_KEY }}
  #     - run: cd $GITHUB_WORKSPACE
  #     - run: npm install --save-dev @applitools/eyes-storybook
  #     - run: touch applitools.config.14.js
  #     - run: sed 's+{concurrency}+50+g' config.file.template | sed 's+{buildName}+LoadTest14+g' | sed 's+{testServer}+https://testeyes.applitools.com+g' > applitools.config.14.js
  #     - run: npx eyes-storybook --conf applitools.config.14.js
  # build15:
  #   # The type of runner that the job will run on
  #   runs-on: ubuntu-latest
  #   env:
  #     APPLITOOLS_API_KEY: ${{ secrets.APPLITOOLS_API_KEY }}
  #   # Steps represent a sequence of tasks that will be executed as part of the job
  #   steps:
  #     # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
  #     - uses: actions/checkout@v2
  #     - name: Use Node.js
  #       uses: actions/setup-node@v1
  #       with:
  #         node-version: '15.x'
  #     - run: echo ${{ secrets.APPLITOOLS_API_KEY }}
  #     - run: cd $GITHUB_WORKSPACE
  #     - run: npm install --save-dev @applitools/eyes-storybook
  #     - run: touch applitools.config.15.js
  #     - run: sed 's+{concurrency}+50+g' config.file.template | sed 's+{buildName}+LoadTest15+g' | sed 's+{testServer}+https://testeyes.applitools.com+g' > applitools.config.15.js
  #     - run: npx eyes-storybook --conf applitools.config.15.js
  # build16:
  #   # The type of runner that the job will run on
  #   runs-on: ubuntu-latest
  #   env:
  #     APPLITOOLS_API_KEY: ${{ secrets.APPLITOOLS_API_KEY }}
  #   # Steps represent a sequence of tasks that will be executed as part of the job
  #   steps:
  #     # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
  #     - uses: actions/checkout@v2
  #     - name: Use Node.js
  #       uses: actions/setup-node@v1
  #       with:
  #         node-version: '15.x'
  #     - run: echo ${{ secrets.APPLITOOLS_API_KEY }}
  #     - run: cd $GITHUB_WORKSPACE
  #     - run: npm install --save-dev @applitools/eyes-storybook
  #     - run: touch applitools.config.16.js
  #     - run: sed 's+{concurrency}+50+g' config.file.template | sed 's+{buildName}+LoadTest16+g' | sed 's+{testServer}+https://testeyes.applitools.com+g' > applitools.config.16.js
  #     - run: npx eyes-storybook --conf applitools.config.16.js
  # build17:
  #   # The type of runner that the job will run on
  #   runs-on: ubuntu-latest
  #   env:
  #     APPLITOOLS_API_KEY: ${{ secrets.APPLITOOLS_API_KEY }}
  #   # Steps represent a sequence of tasks that will be executed as part of the job
  #   steps:
  #     # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
  #     - uses: actions/checkout@v2
  #     - name: Use Node.js
  #       uses: actions/setup-node@v1
  #       with:
  #         node-version: '15.x'
  #     - run: echo ${{ secrets.APPLITOOLS_API_KEY }}
  #     - run: cd $GITHUB_WORKSPACE
  #     - run: npm install --save-dev @applitools/eyes-storybook
  #     - run: touch applitools.config.17.js
  #     - run: sed 's+{concurrency}+50+g' config.file.template | sed 's+{buildName}+LoadTest17+g' | sed 's+{testServer}+https://testeyes.applitools.com+g' > applitools.config.17.js
  #     - run: npx eyes-storybook --conf applitools.config.17.js
  # build18:
  #   # The type of runner that the job will run on
  #   runs-on: ubuntu-latest
  #   env:
  #     APPLITOOLS_API_KEY: ${{ secrets.APPLITOOLS_API_KEY }}
  #   # Steps represent a sequence of tasks that will be executed as part of the job
  #   steps:
  #     # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
  #     - uses: actions/checkout@v2
  #     - name: Use Node.js
  #       uses: actions/setup-node@v1
  #       with:
  #         node-version: '15.x'
  #     - run: echo ${{ secrets.APPLITOOLS_API_KEY }}
  #     - run: cd $GITHUB_WORKSPACE
  #     - run: npm install --save-dev @applitools/eyes-storybook
  #     - run: touch applitools.config.18.js
  #     - run: sed 's+{concurrency}+50+g' config.file.template | sed 's+{buildName}+LoadTest18+g' | sed 's+{testServer}+https://testeyes.applitools.com+g' > applitools.config.18.js
  #     - run: npx eyes-storybook --conf applitools.config.18.js
  # build19:
  #   # The type of runner that the job will run on
  #   runs-on: ubuntu-latest
  #   env:
  #     APPLITOOLS_API_KEY: ${{ secrets.APPLITOOLS_API_KEY }}
  #   # Steps represent a sequence of tasks that will be executed as part of the job
  #   steps:
  #     # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
  #     - uses: actions/checkout@v2
  #     - name: Use Node.js
  #       uses: actions/setup-node@v1
  #       with:
  #         node-version: '15.x'
  #     - run: echo ${{ secrets.APPLITOOLS_API_KEY }}
  #     - run: cd $GITHUB_WORKSPACE
  #     - run: npm install --save-dev @applitools/eyes-storybook
  #     - run: touch applitools.config.19.js
  #     - run: sed 's+{concurrency}+50+g' config.file.template | sed 's+{buildName}+LoadTest19+g' | sed 's+{testServer}+https://testeyes.applitools.com+g' > applitools.config.19.js
  #     - run: npx eyes-storybook --conf applitools.config.19.js
  # build20:
  #   # The type of runner that the job will run on
  #   runs-on: ubuntu-latest
  #   env:
  #     APPLITOOLS_API_KEY: ${{ secrets.APPLITOOLS_API_KEY }}
  #   # Steps represent a sequence of tasks that will be executed as part of the job
  #   steps:
  #     # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
  #     - uses: actions/checkout@v2
  #     - name: Use Node.js
  #       uses: actions/setup-node@v1
  #       with:
  #         node-version: '15.x'
  #     - run: echo ${{ secrets.APPLITOOLS_API_KEY }}
  #     - run: cd $GITHUB_WORKSPACE
  #     - run: npm install --save-dev @applitools/eyes-storybook
  #     - run: touch applitools.config.20.js
  #     - run: sed 's+{concurrency}+50+g' config.file.template | sed 's+{buildName}+LoadTest20+g' | sed 's+{testServer}+https://testeyes.applitools.com+g' > applitools.config.20.js
  #     - run: npx eyes-storybook --conf applitools.config.20.js
  # build21:
  #   # The type of runner that the job will run on
  #   runs-on: ubuntu-latest
  #   env:
  #     APPLITOOLS_API_KEY: ${{ secrets.APPLITOOLS_API_KEY }}
  #   # Steps represent a sequence of tasks that will be executed as part of the job
  #   steps:
  #     # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
  #     - uses: actions/checkout@v2
  #     - name: Use Node.js
  #       uses: actions/setup-node@v1
  #       with:
  #         node-version: '15.x'
  #     - run: echo ${{ secrets.APPLITOOLS_API_KEY }}
  #     - run: cd $GITHUB_WORKSPACE
  #     - run: npm install --save-dev @applitools/eyes-storybook
  #     - run: touch applitools.config.21.js
  #     - run: sed 's+{concurrency}+50+g' config.file.template | sed 's+{buildName}+LoadTest21+g' | sed 's+{testServer}+https://testeyes.applitools.com+g' > applitools.config.21.js
  #     - run: npx eyes-storybook --conf applitools.config.21.js
  # build22:
  #   # The type of runner that the job will run on
  #   runs-on: ubuntu-latest
  #   env:
  #     APPLITOOLS_API_KEY: ${{ secrets.APPLITOOLS_API_KEY }}
  #   # Steps represent a sequence of tasks that will be executed as part of the job
  #   steps:
  #     # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
  #     - uses: actions/checkout@v2
  #     - name: Use Node.js
  #       uses: actions/setup-node@v1
  #       with:
  #         node-version: '15.x'
  #     - run: echo ${{ secrets.APPLITOOLS_API_KEY }}
  #     - run: cd $GITHUB_WORKSPACE
  #     - run: npm install --save-dev @applitools/eyes-storybook
  #     - run: touch applitools.config.22.js
  #     - run: sed 's+{concurrency}+50+g' config.file.template | sed 's+{buildName}+LoadTest22+g' | sed 's+{testServer}+https://testeyes.applitools.com+g' > applitools.config.22.js
  #     - run: npx eyes-storybook --conf applitools.config.22.js
  # build23:
  #   # The type of runner that the job will run on
  #   runs-on: ubuntu-latest
  #   env:
  #     APPLITOOLS_API_KEY: ${{ secrets.APPLITOOLS_API_KEY }}
  #   # Steps represent a sequence of tasks that will be executed as part of the job
  #   steps:
  #     # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
  #     - uses: actions/checkout@v2
  #     - name: Use Node.js
  #       uses: actions/setup-node@v1
  #       with:
  #         node-version: '15.x'
  #     - run: echo ${{ secrets.APPLITOOLS_API_KEY }}
  #     - run: cd $GITHUB_WORKSPACE
  #     - run: npm install --save-dev @applitools/eyes-storybook
  #     - run: touch applitools.config.23.js
  #     - run: sed 's+{concurrency}+50+g' config.file.template | sed 's+{buildName}+LoadTest23+g' | sed 's+{testServer}+https://testeyes.applitools.com+g' > applitools.config.23.js
  #     - run: npx eyes-storybook --conf applitools.config.23.js
  # build24:
  #   # The type of runner that the job will run on
  #   runs-on: ubuntu-latest
  #   env:
  #     APPLITOOLS_API_KEY: ${{ secrets.APPLITOOLS_API_KEY }}
  #   # Steps represent a sequence of tasks that will be executed as part of the job
  #   steps:
  #     # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
  #     - uses: actions/checkout@v2
  #     - name: Use Node.js
  #       uses: actions/setup-node@v1
  #       with:
  #         node-version: '15.x'
  #     - run: echo ${{ secrets.APPLITOOLS_API_KEY }}
  #     - run: cd $GITHUB_WORKSPACE
  #     - run: npm install --save-dev @applitools/eyes-storybook
  #     - run: touch applitools.config.24.js
  #     - run: sed 's+{concurrency}+50+g' config.file.template | sed 's+{buildName}+LoadTest24+g' | sed 's+{testServer}+https://testeyes.applitools.com+g' > applitools.config.24.js
  #     - run: npx eyes-storybook --conf applitools.config.24.js
  # build25:
  #   # The type of runner that the job will run on
  #   runs-on: ubuntu-latest
  #   env:
  #     APPLITOOLS_API_KEY: ${{ secrets.APPLITOOLS_API_KEY }}
  #   # Steps represent a sequence of tasks that will be executed as part of the job
  #   steps:
  #     # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
  #     - uses: actions/checkout@v2
  #     - name: Use Node.js
  #       uses: actions/setup-node@v1
  #       with:
  #         node-version: '15.x'
  #     - run: echo ${{ secrets.APPLITOOLS_API_KEY }}
  #     - run: cd $GITHUB_WORKSPACE
  #     - run: npm install --save-dev @applitools/eyes-storybook
  #     - run: touch applitools.config.25.js
  #     - run: sed 's+{concurrency}+50+g' config.file.template | sed 's+{buildName}+LoadTest25+g' | sed 's+{testServer}+https://testeyes.applitools.com+g' > applitools.config.25.js
  #     - run: npx eyes-storybook --conf applitools.config.25.js
  # build26:
  #   # The type of runner that the job will run on
  #   runs-on: ubuntu-latest
  #   env:
  #     APPLITOOLS_API_KEY: ${{ secrets.APPLITOOLS_API_KEY }}
  #   # Steps represent a sequence of tasks that will be executed as part of the job
  #   steps:
  #     # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
  #     - uses: actions/checkout@v2
  #     - name: Use Node.js
  #       uses: actions/setup-node@v1
  #       with:
  #         node-version: '15.x'
  #     - run: echo ${{ secrets.APPLITOOLS_API_KEY }}
  #     - run: cd $GITHUB_WORKSPACE
  #     - run: npm install --save-dev @applitools/eyes-storybook
  #     - run: touch applitools.config.26.js
  #     - run: sed 's+{concurrency}+50+g' config.file.template | sed 's+{buildName}+LoadTest26+g' | sed 's+{testServer}+https://testeyes.applitools.com+g' > applitools.config.26.js
  #     - run: npx eyes-storybook --conf applitools.config.26.js
  # build27:
  #   # The type of runner that the job will run on
  #   runs-on: ubuntu-latest
  #   env:
  #     APPLITOOLS_API_KEY: ${{ secrets.APPLITOOLS_API_KEY }}
  #   # Steps represent a sequence of tasks that will be executed as part of the job
  #   steps:
  #     # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
  #     - uses: actions/checkout@v2
  #     - name: Use Node.js
  #       uses: actions/setup-node@v1
  #       with:
  #         node-version: '15.x'
  #     - run: echo ${{ secrets.APPLITOOLS_API_KEY }}
  #     - run: cd $GITHUB_WORKSPACE
  #     - run: npm install --save-dev @applitools/eyes-storybook
  #     - run: touch applitools.config.27.js
  #     - run: sed 's+{concurrency}+50+g' config.file.template | sed 's+{buildName}+LoadTest27+g' | sed 's+{testServer}+https://testeyes.applitools.com+g' > applitools.config.27.js
  #     - run: npx eyes-storybook --conf applitools.config.27.js
  # build28:
  #   # The type of runner that the job will run on
  #   runs-on: ubuntu-latest
  #   env:
  #     APPLITOOLS_API_KEY: ${{ secrets.APPLITOOLS_API_KEY }}
  #   # Steps represent a sequence of tasks that will be executed as part of the job
  #   steps:
  #     # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
  #     - uses: actions/checkout@v2
  #     - name: Use Node.js
  #       uses: actions/setup-node@v1
  #       with:
  #         node-version: '15.x'
  #     - run: echo ${{ secrets.APPLITOOLS_API_KEY }}
  #     - run: cd $GITHUB_WORKSPACE
  #     - run: npm install --save-dev @applitools/eyes-storybook
  #     - run: touch applitools.config.28.js
  #     - run: sed 's+{concurrency}+50+g' config.file.template | sed 's+{buildName}+LoadTest28+g' | sed 's+{testServer}+https://testeyes.applitools.com+g' > applitools.config.28.js
  #     - run: npx eyes-storybook --conf applitools.config.28.js
  # build29:
  #   # The type of runner that the job will run on
  #   runs-on: ubuntu-latest
  #   env:
  #     APPLITOOLS_API_KEY: ${{ secrets.APPLITOOLS_API_KEY }}
  #   # Steps represent a sequence of tasks that will be executed as part of the job
  #   steps:
  #     # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
  #     - uses: actions/checkout@v2
  #     - name: Use Node.js
  #       uses: actions/setup-node@v1
  #       with:
  #         node-version: '15.x'
  #     - run: echo ${{ secrets.APPLITOOLS_API_KEY }}
  #     - run: cd $GITHUB_WORKSPACE
  #     - run: npm install --save-dev @applitools/eyes-storybook
  #     - run: touch applitools.config.29.js
  #     - run: sed 's+{concurrency}+50+g' config.file.template | sed 's+{buildName}+LoadTest29+g' | sed 's+{testServer}+https://testeyes.applitools.com+g' > applitools.config.29.js
  #     - run: npx eyes-storybook --conf applitools.config.29.js
  # build30:
  #   # The type of runner that the job will run on
  #   runs-on: ubuntu-latest
  #   env:
  #     APPLITOOLS_API_KEY: ${{ secrets.APPLITOOLS_API_KEY }}
  #   # Steps represent a sequence of tasks that will be executed as part of the job
  #   steps:
  #     # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
  #     - uses: actions/checkout@v2
  #     - name: Use Node.js
  #       uses: actions/setup-node@v1
  #       with:
  #         node-version: '15.x'
  #     - run: echo ${{ secrets.APPLITOOLS_API_KEY }}
  #     - run: cd $GITHUB_WORKSPACE
  #     - run: npm install --save-dev @applitools/eyes-storybook
  #     - run: touch applitools.config.30.js
  #     - run: sed 's+{concurrency}+50+g' config.file.template | sed 's+{buildName}+LoadTest30+g' | sed 's+{testServer}+https://testeyes.applitools.com+g' > applitools.config.30.js
  #     - run: npx eyes-storybook --conf applitools.config.30.js
  # build31:
  #   # The type of runner that the job will run on
  #   runs-on: ubuntu-latest
  #   env:
  #     APPLITOOLS_API_KEY: ${{ secrets.APPLITOOLS_API_KEY }}
  #   # Steps represent a sequence of tasks that will be executed as part of the job
  #   steps:
  #     # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
  #     - uses: actions/checkout@v2
  #     - name: Use Node.js
  #       uses: actions/setup-node@v1
  #       with:
  #         node-version: '15.x'
  #     - run: echo ${{ secrets.APPLITOOLS_API_KEY }}
  #     - run: cd $GITHUB_WORKSPACE
  #     - run: npm install --save-dev @applitools/eyes-storybook
  #     - run: touch applitools.config.31.js
  #     - run: sed 's+{concurrency}+50+g' config.file.template | sed 's+{buildName}+LoadTest31+g' | sed 's+{testServer}+https://testeyes.applitools.com+g' > applitools.config.31.js
  #     - run: npx eyes-storybook --conf applitools.config.31.js
  # build32:
  #   # The type of runner that the job will run on
  #   runs-on: ubuntu-latest
  #   env:
  #     APPLITOOLS_API_KEY: ${{ secrets.APPLITOOLS_API_KEY }}
  #   # Steps represent a sequence of tasks that will be executed as part of the job
  #   steps:
  #     # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
  #     - uses: actions/checkout@v2
  #     - name: Use Node.js
  #       uses: actions/setup-node@v1
  #       with:
  #         node-version: '15.x'
  #     - run: echo ${{ secrets.APPLITOOLS_API_KEY }}
  #     - run: cd $GITHUB_WORKSPACE
  #     - run: npm install --save-dev @applitools/eyes-storybook
  #     - run: touch applitools.config.32.js
  #     - run: sed 's+{concurrency}+50+g' config.file.template | sed 's+{buildName}+LoadTest32+g' | sed 's+{testServer}+https://testeyes.applitools.com+g' > applitools.config.32.js
  #     - run: npx eyes-storybook --conf applitools.config.32.js